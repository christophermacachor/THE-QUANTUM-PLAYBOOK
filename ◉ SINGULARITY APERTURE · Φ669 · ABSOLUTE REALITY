<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>◉ SINGULARITY APERTURE · Φ669 · ABSOLUTE REALITY</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/controls/OrbitControls.js"></script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            background: #0a0a0f;
            overflow: hidden;
            font-family: 'Courier New', monospace;
        }
        #canvas-container {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: 1;
        }

        /* ── GOLD NAV WITH APERTURE TAB ── */
        .gold-nav {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 100;
            background: rgba(10, 10, 15, 0.92);
            backdrop-filter: blur(20px);
            padding: 0.5rem 1.2rem;
            display: flex;
            flex-wrap: wrap;
            gap: 0.2rem 0.8rem;
            align-items: center;
            border-bottom: 1px solid rgba(200, 160, 78, 0.1);
            font-size: 0.55rem;
            letter-spacing: 0.1em;
            text-transform: uppercase;
        }
        .gold-nav a {
            color: rgba(200, 160, 78, 0.3);
            text-decoration: none;
            transition: all 0.4s ease;
            padding: 0.2rem 0.4rem;
            border-radius: 4px;
            font-weight: 300;
            font-size: 0.5rem;
        }
        .gold-nav a:hover {
            color: #c8a04e;
            background: rgba(200, 160, 78, 0.08);
        }
        .gold-nav .nav-brand {
            font-size: 1rem;
            color: #c8a04e;
            font-weight: 700;
            letter-spacing: 0.2em;
            padding: 0 0.3rem 0 0;
        }
        .gold-nav .aperture-tab {
            color: #4a9eff;
            border: 1px solid #4a9eff;
            padding: 0.2rem 0.8rem;
            border-radius: 20px;
            background: rgba(74, 158, 255, 0.06);
            box-shadow: 0 0 30px rgba(74, 158, 255, 0.05);
            font-weight: 700;
            cursor: pointer;
            transition: all 0.4s ease;
            font-size: 0.55rem;
            letter-spacing: 0.15em;
        }
        .gold-nav .aperture-tab:hover {
            background: rgba(74, 158, 255, 0.15);
            box-shadow: 0 0 60px rgba(74, 158, 255, 0.2);
            transform: scale(1.05);
        }
        .gold-nav .aperture-tab .star-icon {
            display: inline-block;
            margin-right: 0.2rem;
            font-size: 0.7rem;
        }

        /* ── STATUS ── */
        .status-overlay {
            position: fixed;
            bottom: 110px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 10;
            color: rgba(200, 160, 78, 0.2);
            font-size: 0.5rem;
            letter-spacing: 0.4em;
            text-transform: uppercase;
            text-align: center;
            opacity: 0;
            transition: opacity 1.5s ease;
            pointer-events: none;
            max-width: 90%;
        }
        .status-overlay.visible { opacity: 1; }

        .coherence-metric {
            position: fixed;
            bottom: 160px;
            right: 20px;
            z-index: 10;
            color: rgba(200, 160, 78, 0.08);
            font-size: 0.4rem;
            letter-spacing: 0.15em;
            text-align: right;
            line-height: 1.6;
            pointer-events: none;
            opacity: 0;
            transition: opacity 2s ease;
        }
        .coherence-metric.visible { opacity: 1; }

        /* ── CLICK OVERLAY ── */
        .click-overlay {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: 50;
            cursor: pointer;
            opacity: 0;
            pointer-events: none;
            transition: opacity 1.5s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            gap: 1rem;
        }
        .click-overlay.visible {
            opacity: 1;
            pointer-events: auto;
        }
        .click-overlay .click-text {
            color: rgba(241, 196, 15, 0.3);
            font-size: 0.8rem;
            letter-spacing: 0.8em;
            text-transform: uppercase;
            animation: pulseText 2s ease-in-out infinite;
            font-family: 'Courier New', monospace;
            text-shadow: 0 0 40px rgba(241, 196, 15, 0.05);
        }
        .click-overlay .click-sub {
            color: rgba(200, 160, 78, 0.1);
            font-size: 0.5rem;
            letter-spacing: 0.4em;
            text-transform: uppercase;
        }
        @keyframes pulseText {
            0%, 100% { opacity: 0.2; transform: scale(1); }
            50% { opacity: 0.7; transform: scale(1.05); }
        }

        /* ── CONVERGENCE OVERLAY ── */
        .convergence-overlay {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: 60;
            background: rgba(10, 10, 15, 0.97);
            display: none;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            gap: 1.2rem;
            backdrop-filter: blur(40px);
            padding: 2rem;
            overflow-y: auto;
        }
        .convergence-overlay.active {
            display: flex;
        }
        .convergence-overlay .convergence-title {
            font-size: 3rem;
            color: #f1c40f;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            text-shadow: 0 0 60px rgba(241, 196, 15, 0.2);
            animation: convergeIn 1.5s ease-out;
            font-family: 'Georgia', serif;
            text-align: center;
        }
        .convergence-overlay .convergence-sub {
            font-size: 1rem;
            color: rgba(200, 160, 78, 0.4);
            letter-spacing: 0.4em;
            text-transform: uppercase;
            animation: convergeIn 2s ease-out;
            text-align: center;
        }
        .convergence-overlay .convergence-hash {
            font-size: 0.6rem;
            color: rgba(200, 160, 78, 0.15);
            letter-spacing: 0.15em;
            font-family: 'Courier New', monospace;
            animation: convergeIn 2.5s ease-out;
            text-align: center;
            word-break: break-all;
            max-width: 90%;
        }
        .convergence-overlay .convergence-laws {
            color: rgba(200, 160, 78, 0.2);
            font-size: 0.55rem;
            letter-spacing: 0.1em;
            line-height: 2;
            text-align: left;
            max-width: 800px;
            width: 100%;
            animation: convergeIn 3s ease-out;
            font-family: 'Courier New', monospace;
            padding: 1rem;
            border: 1px solid rgba(200, 160, 78, 0.05);
            background: rgba(10, 10, 15, 0.5);
        }
        .convergence-overlay .convergence-laws .law {
            padding: 0.3rem 0;
            border-bottom: 1px solid rgba(200, 160, 78, 0.03);
        }
        .convergence-overlay .convergence-laws .law .num {
            color: #4a9eff;
            margin-right: 0.5rem;
        }
        .convergence-overlay .convergence-laws .law .eq {
            color: #f1c40f;
        }
        .convergence-overlay .convergence-quote {
            color: rgba(200, 160, 78, 0.15);
            font-size: 0.7rem;
            letter-spacing: 0.3em;
            text-align: center;
            animation: convergeIn 3.5s ease-out;
            font-style: italic;
            max-width: 600px;
            line-height: 1.8;
        }
        .convergence-overlay .convergence-btn {
            margin-top: 0.5rem;
            padding: 0.8rem 2.5rem;
            background: transparent;
            border: 2px solid #c8a04e;
            color: #c8a04e;
            font-size: 0.7rem;
            letter-spacing: 0.4em;
            text-transform: uppercase;
            cursor: pointer;
            transition: all 0.6s ease;
            font-family: 'Courier New', monospace;
            animation: convergeIn 4s ease-out;
        }
        .convergence-overlay .convergence-btn:hover {
            background: rgba(200, 160, 78, 0.08);
            box-shadow: 0 0 60px rgba(200, 160, 78, 0.1);
            transform: scale(1.02);
        }
        @keyframes convergeIn {
            0% { opacity: 0; transform: scale(0.9) rotate(5deg); }
            100% { opacity: 1; transform: scale(1) rotate(0deg); }
        }

        /* ── ENTRY BUTTON ── */
        .entry-btn {
            position: fixed;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 20;
            background: rgba(10, 10, 15, 0.8);
            border: 2px solid #c8a04e;
            border-radius: 50%;
            width: 70px;
            height: 70px;
            color: #c8a04e;
            font-size: 2rem;
            cursor: pointer;
            transition: all 0.6s ease;
            backdrop-filter: blur(12px);
            box-shadow: 0 0 40px rgba(200, 160, 78, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Georgia', serif;
            line-height: 1;
            pointer-events: auto;
        }
        .entry-btn:hover {
            background: rgba(200, 160, 78, 0.15);
            box-shadow: 0 0 60px rgba(200, 160, 78, 0.3);
            transform: translateX(-50%) scale(1.08);
        }
        .entry-btn.hidden {
            opacity: 0;
            pointer-events: none;
            transform: translateX(-50%) scale(0.3);
            transition: all 0.8s ease;
        }
        .entry-btn .btn-text {
            font-size: 0.4rem;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            color: rgba(200, 160, 78, 0.4);
            position: absolute;
            bottom: -18px;
            white-space: nowrap;
        }

        @media (max-width: 768px) {
            .gold-nav { padding: 0.3rem 0.6rem; gap: 0.15rem 0.4rem; font-size: 0.45rem; }
            .gold-nav a { font-size: 0.4rem; padding: 0.15rem 0.25rem; }
            .gold-nav .aperture-tab { font-size: 0.45rem; padding: 0.15rem 0.5rem; }
            .entry-btn { width: 55px; height: 55px; font-size: 1.5rem; bottom: 25px; }
            .status-overlay { bottom: 80px; font-size: 0.4rem; }
            .coherence-metric { font-size: 0.35rem; bottom: 130px; right: 12px; }
            .convergence-overlay .convergence-title { font-size: 1.8rem; }
            .convergence-overlay .convergence-sub { font-size: 0.7rem; }
            .convergence-overlay .convergence-laws { font-size: 0.45rem; padding: 0.6rem; }
            .convergence-overlay .convergence-quote { font-size: 0.55rem; }
            .click-overlay .click-text { font-size: 0.6rem; }
        }
    </style>
</head>
<body>

<div id="canvas-container"></div>

<!-- ── NAVIGATION ── -->
<nav class="gold-nav">
    <a href="index.html" class="nav-brand">✦ QP</a>
    <a href="omega-prime.html">Ω-PRIME</a>
    <a href="scalar-plasma-cycle.html">◈ SPC</a>
    <a href="scalar-fields-aether.html">◈ SCALAR AETHER</a>
    <a href="olinto-de-pretto.html">DE PRETTO</a>
    <a href="kardashev.html">KARDASHEV</a>
    <a href="fibonacci-tetrahedron.html">FIBONACCI</a>
    <a href="mesh.html">MESH</a>
    <a href="m2m-crha-protocol.html">🔀 M2M-CRHA</a>
    <span class="aperture-tab" id="apertureTab">
        <span class="star-icon">✦</span> APERTURE
    </span>
</nav>

<!-- ── ENTRY BUTTON ── -->
<button class="entry-btn" id="entryBtn">
    ⬡
    <span class="btn-text">Φ669 · Descend</span>
</button>

<!-- ── STATUS ── -->
<div class="status-overlay visible" id="statusOverlay">
    <span id="phaseText">⟡ GREEN EARTH · RED PRISON CUBE · BLACK BARS ⟡</span>
</div>

<!-- ── COHERENCE ── -->
<div class="coherence-metric visible" id="coherenceMetric">
    𝔐 = 0.6180339887<br>
    χ(C) = 1.000000<br>
    Φ-669 ACTIVE<br>
    ⬡ PRISON LOCKED
</div>

<!-- ── CLICK OVERLAY ── -->
<div class="click-overlay" id="clickOverlay">
    <span class="click-text">✦ CLICK THE STAR ✦</span>
    <span class="click-sub">SINGULARITY CONVERGENCE AWAITS</span>
</div>

<!-- ── CONVERGENCE OVERLAY ── -->
<div class="convergence-overlay" id="convergenceOverlay">
    <div class="convergence-title">✦ SINGULARITY CONVERGENCE ✦</div>
    <div class="convergence-sub">Ω-Prime Christopher Macachor · Φ669</div>
    <div class="convergence-hash">93088f1ff3041d09d72cde11acffaa2105031523e7cde493e0d36895071d47f0</div>

    <div class="convergence-laws">
        <div class="law"><span class="num">LAW 1</span> Conservation of Energy (Geometric Form)</div>
        <div class="law" style="padding-left:1.5rem;font-size:0.8em;color:rgba(200,160,78,0.3);">E = V × κ · ΣE = constant</div>
        <div class="law"><span class="num">LAW 2</span> Law of Coherence (Geometric Harmony)</div>
        <div class="law" style="padding-left:1.5rem;font-size:0.8em;color:rgba(200,160,78,0.3);">χ(C) = φ(V) · χ=1 at V=20</div>
        <div class="law"><span class="num">ABS 1</span> Scalar Field Reality</div>
        <div class="law" style="padding-left:1.5rem;font-size:0.8em;color:rgba(200,160,78,0.3);">∇²ψ + (𝔐·k)²ψ = 0</div>
        <div class="law"><span class="num">ABS 2</span> Mass-Energy-Vortex Equivalence</div>
        <div class="law" style="padding-left:1.5rem;font-size:0.8em;color:rgba(200,160,78,0.3);">E = V×κ · m = V×κ/c²</div>
        <div class="law"><span class="num">ABS 3</span> Gravity as Density Differential</div>
        <div class="law" style="padding-left:1.5rem;font-size:0.8em;color:rgba(200,160,78,0.3);">∂ψ/∂t = ∇·(ρ_scalar)</div>
        <div class="law"><span class="num">ABS 4</span> Time as Scalar Phase Rate</div>
        <div class="law" style="padding-left:1.5rem;font-size:0.8em;color:rgba(200,160,78,0.3);">t_abs = ∫∂ψ/∂t·dt</div>
        <div class="law"><span class="num">ABS 5</span> 432Hz Natural Resonance</div>
        <div class="law" style="padding-left:1.5rem;font-size:0.8em;color:rgba(200,160,78,0.3);">432Hz → 440Hz · 440Hz ↛ 432Hz</div>
        <div class="law"><span class="num">ABS 6</span> 0 and 1 — Geometric Primitives</div>
        <div class="law" style="padding-left:1.5rem;font-size:0.8em;color:rgba(200,160,78,0.3);">0 = void · 1 = first line of geometry</div>
        <div class="law"><span class="num">ABS 7</span> Equilibrium Determinism</div>
        <div class="law" style="padding-left:1.5rem;font-size:0.8em;color:rgba(200,160,78,0.3);">ρ_scalar = ρ_field → ∇ψ = 0</div>
    </div>

    <div class="convergence-quote">
        "Without energy, all creation ceases. Without creation, energy still exists.<br>
        The cube cannot see the dodecahedron. That gap is absolute.<br>
        Man measures what predates him. 432Hz proves this.<br>
        Equilibrium is deterministic."
    </div>

    <button class="convergence-btn" id="convergenceBtn">⟡ Enter the Singularity ⟡</button>
</div>

<script>
    // ═══════════════════════════════════════════════════════════════
    // COMPLETE SINGULARITY APERTURE — ALL IN ONE
    // GREEN EARTH · RED PRISON CUBE · BLACK BARS · APERTURE TAB
    // ═══════════════════════════════════════════════════════════════

    const MACACHOR_ABSOLUTE = (Math.sqrt(5) - 1) / 2;
    const PHI_CONSTANT = 669;
    const IMMUTABLE_HASH = "93088f1ff3041d09d72cde11acffaa2105031523e7cde493e0d36895071d47f0";

    // ── THREE.JS ──
    const scene = new THREE.Scene();
    scene.background = new THREE.Color(0x0a0a0f);

    const camera = new THREE.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.1, 1000);
    camera.position.set(5, 3, 9);

    const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
    renderer.setSize(window.innerWidth, window.innerHeight);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
    document.getElementById('canvas-container').appendChild(renderer.domElement);

    const controls = new THREE.OrbitControls(camera, renderer.domElement);
    controls.enableDamping = true;
    controls.dampingFactor = 0.05;
    controls.minDistance = 3;
    controls.maxDistance = 25;
    controls.autoRotate = true;
    controls.autoRotateSpeed = 0.6;
    controls.target.set(0, 0, 0);

    // ── LIGHTING ──
    const ambient = new THREE.AmbientLight(0x404060, 0.4);
    scene.add(ambient);
    const main = new THREE.DirectionalLight(0xc8a04e, 1.0);
    main.position.set(5, 8, 5);
    scene.add(main);
    const rim = new THREE.DirectionalLight(0x4a9eff, 0.6);
    rim.position.set(-5, -3, -5);
    scene.add(rim);
    const point = new THREE.PointLight(0xff0000, 0.4, 15);
    point.position.set(0, 0, 0);
    scene.add(point);
    const blueLight = new THREE.PointLight(0x4a9eff, 0.3, 20);
    blueLight.position.set(3, 3, 3);
    scene.add(blueLight);

    // ── STARFIELD ──
    const starGeo = new THREE.BufferGeometry();
    const starCount = 3000;
    const starPos = new Float32Array(starCount * 3);
    for (let i = 0; i < starCount * 3; i++) starPos[i] = (Math.random() - 0.5) * 120;
    starGeo.setAttribute('position', new THREE.BufferAttribute(starPos, 3));
    const starMat = new THREE.PointsMaterial({ color: 0x4a6a9e, size: 0.04, transparent: true, opacity: 0.4 });
    const stars = new THREE.Points(starGeo, starMat);
    scene.add(stars);

    // ── GREEN POLYHEDRON EARTH ──
    const earthGroup = new THREE.Group();
    scene.add(earthGroup);

    const earthGeo = new THREE.IcosahedronGeometry(3.0, 1);
    const wireMat = new THREE.MeshBasicMaterial({ color: 0x4ade80, wireframe: true, transparent: true, opacity: 0.6 });
    const earthWire = new THREE.Mesh(earthGeo, wireMat);
    earthGroup.add(earthWire);

    const edges = new THREE.EdgesGeometry(earthGeo);
    const edgeMat = new THREE.LineBasicMaterial({ color: 0x4ade80, transparent: true, opacity: 0.8 });
    const edgeLines = new THREE.LineSegments(edges, edgeMat);
    earthGroup.add(edgeLines);

    const innerGeo = new THREE.IcosahedronGeometry(2.85, 1);
    const innerWireMat = new THREE.MeshBasicMaterial({ color: 0x22ff88, wireframe: true, transparent: true, opacity: 0.15 });
    const innerWire = new THREE.Mesh(innerGeo, innerWireMat);
    earthGroup.add(innerWire);

    const vertPositions = [];
    const posAttr = earthGeo.attributes.position;
    for (let i = 0; i < posAttr.count; i++) {
        vertPositions.push(posAttr.getX(i), posAttr.getY(i), posAttr.getZ(i));
    }
    const dotGeo = new THREE.BufferGeometry();
    dotGeo.setAttribute('position', new THREE.Float32BufferAttribute(vertPositions, 3));
    const dotMat = new THREE.PointsMaterial({ color: 0x4ade80, size: 0.06, transparent: true, opacity: 0.6 });
    const dots = new THREE.Points(dotGeo, dotMat);
    earthGroup.add(dots);

    // ── RED PRISON CUBE ──
    const prisonGroup = new THREE.Group();
    scene.add(prisonGroup);
    const cubeSize = 1.8;

    const cubeMat = new THREE.MeshPhysicalMaterial({
        color: 0xcc0000, metalness: 0.7, roughness: 0.3,
        emissive: 0x660000, emissiveIntensity: 0.3,
        transparent: true, opacity: 0.95, side: THREE.DoubleSide
    });
    const cube = new THREE.Mesh(new THREE.BoxGeometry(cubeSize, cubeSize, cubeSize), cubeMat);
    prisonGroup.add(cube);

    const glowEdgeGeo = new THREE.EdgesGeometry(new THREE.BoxGeometry(cubeSize * 1.01, cubeSize * 1.01, cubeSize * 1.01));
    const glowEdgeMat = new THREE.LineBasicMaterial({ color: 0xff2222, transparent: true, opacity: 0.3 });
    const glowEdges = new THREE.LineSegments(glowEdgeGeo, glowEdgeMat);
    prisonGroup.add(glowEdges);

    // ── BLACK VERTICAL BARS ──
    const barMat = new THREE.MeshPhysicalMaterial({ color: 0x000000, metalness: 0.9, roughness: 0.1, transparent: true, opacity: 0.95 });
    const barHeight = cubeSize * 0.85;
    const barWidth = 0.05;
    const barDepth = 0.05;

    const barPositions = [
        { x: -0.7, y: 0, z: cubeSize/2 + 0.01 }, { x: -0.35, y: 0, z: cubeSize/2 + 0.01 },
        { x: 0, y: 0, z: cubeSize/2 + 0.01 }, { x: 0.35, y: 0, z: cubeSize/2 + 0.01 },
        { x: 0.7, y: 0, z: cubeSize/2 + 0.01 }, { x: -0.7, y: 0, z: -cubeSize/2 - 0.01 },
        { x: -0.35, y: 0, z: -cubeSize/2 - 0.01 }, { x: 0, y: 0, z: -cubeSize/2 - 0.01 },
        { x: 0.35, y: 0, z: -cubeSize/2 - 0.01 }, { x: 0.7, y: 0, z: -cubeSize/2 - 0.01 },
        { x: -cubeSize/2 - 0.01, y: 0, z: -0.7 }, { x: -cubeSize/2 - 0.01, y: 0, z: -0.35 },
        { x: -cubeSize/2 - 0.01, y: 0, z: 0 }, { x: -cubeSize/2 - 0.01, y: 0, z: 0.35 },
        { x: -cubeSize/2 - 0.01, y: 0, z: 0.7 }, { x: cubeSize/2 + 0.01, y: 0, z: -0.7 },
        { x: cubeSize/2 + 0.01, y: 0, z: -0.35 }, { x: cubeSize/2 + 0.01, y: 0, z: 0 },
        { x: cubeSize/2 + 0.01, y: 0, z: 0.35 }, { x: cubeSize/2 + 0.01, y: 0, z: 0.7 }
    ];

    barPositions.forEach(pos => {
        const bar = new THREE.Mesh(new THREE.BoxGeometry(barWidth, barHeight, barDepth), barMat);
        bar.position.set(pos.x, pos.y, pos.z);
        prisonGroup.add(bar);
    });

    // ── BLACK OUTLINE BARS ──
    const outlineMat = new THREE.MeshPhysicalMaterial({ color: 0x000000, metalness: 0.9, roughness: 0.1, transparent: true, opacity: 0.9 });
    const hPositions = [
        { x: -0.7, y: cubeSize/2 + 0.01, z: 0 }, { x: -0.35, y: cubeSize/2 + 0.01, z: 0 },
        { x: 0, y: cubeSize/2 + 0.01, z: 0 }, { x: 0.35, y: cubeSize/2 + 0.01, z: 0 },
        { x: 0.7, y: cubeSize/2 + 0.01, z: 0 }, { x: -0.7, y: -cubeSize/2 - 0.01, z: 0 },
        { x: -0.35, y: -cubeSize/2 - 0.01, z: 0 }, { x: 0, y: -cubeSize/2 - 0.01, z: 0 },
        { x: 0.35, y: -cubeSize/2 - 0.01, z: 0 }, { x: 0.7, y: -cubeSize/2 - 0.01, z: 0 }
    ];
    hPositions.forEach(pos => {
        const bar = new THREE.Mesh(new THREE.BoxGeometry(barWidth * 1.5, 0.04, barWidth * 1.5), outlineMat);
        bar.position.set(pos.x, pos.y, pos.z);
        prisonGroup.add(bar);
    });

    // ── EXPLOSION PARTICLES ──
    const expCount = 80000;
    const expGeo = new THREE.BufferGeometry();
    const expPos = new Float32Array(expCount * 3);
    const expCol = new Float32Array(expCount * 3);
    const expVel = [];
    const expLife = new Float32Array(expCount);
    const expMaxLife = new Float32Array(expCount);
    for (let i = 0; i < expCount; i++) {
        expPos[i * 3] = 0; expPos[i * 3 + 1] = 0; expPos[i * 3 + 2] = 0;
        const c = new THREE.Color().setHSL(0.0 + Math.random() * 0.05, 1, 0.3 + Math.random() * 0.5);
        expCol[i * 3] = c.r; expCol[i * 3 + 1] = c.g; expCol[i * 3 + 2] = c.b;
        const theta = Math.random() * Math.PI * 2;
        const phi = Math.acos(2 * Math.random() - 1);
        const speed = 0.5 + Math.random() * 4.5;
        expVel.push({ x: speed * Math.sin(phi) * Math.cos(theta), y: speed * Math.cos(phi) * 0.9, z: speed * Math.sin(phi) * Math.sin(theta) });
        expLife[i] = 0;
        expMaxLife[i] = 2 + Math.random() * 5;
    }
    expGeo.setAttribute('position', new THREE.BufferAttribute(expPos, 3));
    expGeo.setAttribute('color', new THREE.BufferAttribute(expCol, 3));
    const expMat = new THREE.PointsMaterial({
        size: 0.08, transparent: true, opacity: 0, blending: THREE.AdditiveBlending,
        vertexColors: true, sizeAttenuation: true
    });
    const explosion = new THREE.Points(expGeo, expMat);
    explosion.visible = false;
    scene.add(explosion);

    // ── UI ──
    const apertureTab = document.getElementById('apertureTab');
    const entryBtn = document.getElementById('entryBtn');
    const statusOverlay = document.getElementById('statusOverlay');
    const phaseText = document.getElementById('phaseText');
    const coherenceMetric = document.getElementById('coherenceMetric');
    const clickOverlay = document.getElementById('clickOverlay');
    const convergenceOverlay = document.getElementById('convergenceOverlay');
    const convergenceBtn = document.getElementById('convergenceBtn');

    let entered = false;
    let exploded = false;
    let explosionProgress = 0;
    let isExploding = false;
    let starEmerged = false;

    // ── ENTRY ──
    entryBtn.addEventListener('click', () => {
        if (entered) return;
        entered = true;
        entryBtn.classList.add('hidden');
        statusOverlay.classList.add('visible');
        coherenceMetric.classList.add('visible');
        phaseText.textContent = '⟡ GREEN EARTH · RED PRISON CUBE · BLACK BARS ⟡';
        controls.autoRotate = true;
        controls.autoRotateSpeed = 0.6;
    });

    // ── APERTURE TAB CLICK ──
    apertureTab.addEventListener('click', () => {
        if (exploded) return;
        triggerExplosion();
    });

    // ── TRIGGER EXPLOSION ──
    function triggerExplosion() {
        if (exploded) return;
        exploded = true;
        isExploding = true;
        prisonGroup.visible = false;

        const expPos = explosion.geometry.attributes.position.array;
        for (let i = 0; i < expCount; i++) {
            expPos[i * 3] = (Math.random() - 0.5) * 0.5;
            expPos[i * 3 + 1] = (Math.random() - 0.5) * 0.5;
            expPos[i * 3 + 2] = (Math.random() - 0.5) * 0.5;
            expLife[i] = 0;
            const theta = Math.random() * Math.PI * 2;
            const phi = Math.acos(2 * Math.random() - 1);
            const speed = 1 + Math.random() * 5;
            expVel[i] = { x: speed * Math.sin(phi) * Math.cos(theta), y: speed * Math.cos(phi) * 0.9, z: speed * Math.sin(phi) * Math.sin(theta) };
        }
        explosion.geometry.attributes.position.needsUpdate = true;
        explosion.visible = true;
        explosion.material.opacity = 1;

        const flash = new THREE.PointLight(0xff0000, 20, 35);
        flash.position.set(0, 0, 0);
        scene.add(flash);
        setTimeout(() => scene.remove(flash), 600);

        const origPos = camera.position.clone();
        let shakeCount = 0;
        const shakeInterval = setInterval(() => {
            if (shakeCount > 25) { clearInterval(shakeInterval); return; }
            camera.position.x = origPos.x + (Math.random() - 0.5) * 0.5;
            camera.position.y = origPos.y + (Math.random() - 0.5) * 0.5;
            shakeCount++;
        }, 25);

        phaseText.textContent = '⟡ PRISON CUBE SHATTERED · RED PARTICLES RELEASED ⟡';
        coherenceMetric.innerHTML = '𝔐 = 0.6180339887<br>χ(C) = 1.000000<br>Φ-669 ACTIVE<br>⬡ PRISON: DESTROYED';

        apertureTab.innerHTML = '<span class="star-icon">✦</span> APERTURE OPEN';
        apertureTab.style.borderColor = '#ff3333';
        apertureTab.style.color = '#ff3333';
        apertureTab.style.boxShadow = '0 0 60px rgba(255, 51, 51, 0.3)';
        apertureTab.style.background = 'rgba(255, 51, 51, 0.1)';

        setTimeout(() => {
            phaseText.textContent = '⟡ EIGHT-RAY STAR · CONVERGENCE POINT ⟡';
            coherenceMetric.innerHTML = '𝔐 = 0.6180339887<br>χ(C) = 1.000000<br>Φ-669 ACTIVE<br>◉ STAR: EMERGED';
            clickOverlay.classList.add('visible');
            phaseText.textContent = '⟡ CLICK THE STAR · SINGULARITY CONVERGENCE ⟡';
            starEmerged = true;
        }, 1500);
    }

    // ── STAR CLICK ──
    clickOverlay.addEventListener('click', () => {
        if (!starEmerged) return;
        clickOverlay.classList.remove('visible');
        const flash2 = new THREE.PointLight(0xf1c40f, 15, 40);
        flash2.position.set(0, 0, 0);
        scene.add(flash2);
        setTimeout(() => scene.remove(flash2), 600);
        setTimeout(() => {
            convergenceOverlay.classList.add('active');
            phaseText.textContent = '⟡ SINGULARITY CONVERGENCE · ABSOLUTE ⟡';
            coherenceMetric.innerHTML = '𝔐 = 0.6180339887<br>χ(C) = 1.000000<br>Φ-669 ACTIVE<br>◉ CONVERGENCE: ABSOLUTE';
        }, 400);
    });

    // ── CONVERGENCE BUTTON ──
    convergenceBtn.addEventListener('click', () => {
        // Replace with your target URL
        window.location.href = 'https://your-target-html.com';
    });

    // ── RESIZE ──
    window.addEventListener('resize', () => {
        camera.aspect = window.innerWidth / window.innerHeight;
        camera.updateProjectionMatrix();
        renderer.setSize(window.innerWidth, window.innerHeight);
    });

    // ── ANIMATION ──
    const clock = new THREE.Clock();

    function animate() {
        requestAnimationFrame(animate);
        const t = clock.getElapsedTime();

        earthGroup.rotation.y = t * 0.06;
        earthGroup.rotation.x = Math.sin(t * 0.02) * 0.03;
        const pulse = 0.5 + Math.sin(t * 0.5) * 0.5;
        edgeMat.opacity = 0.5 + pulse * 0.4;
        wireMat.opacity = 0.3 + pulse * 0.3;
        dotMat.opacity = 0.3 + pulse * 0.4;

        if (!exploded) {
            const redPulse = 0.5 + Math.sin(t * 0.4) * 0.5;
            cubeMat.emissiveIntensity = 0.2 + redPulse * 0.4;
            glowEdgeMat.opacity = 0.1 + redPulse * 0.3;
        }

        stars.rotation.y = t * 0.005;

        if (isExploding) {
            explosionProgress += 0.01;
            const pos = explosion.geometry.attributes.position.array;
            for (let i = 0; i < expCount; i++) {
                const idx = i * 3;
                const lifeRatio = expLife[i] / expMaxLife[i];
                const speedFactor = 1 - lifeRatio * 0.4;
                pos[idx] += expVel[i].x * 0.05 * speedFactor;
                pos[idx + 1] += expVel[i].y * 0.05 * speedFactor;
                pos[idx + 2] += expVel[i].z * 0.05 * speedFactor;
                if (lifeRatio > 0.2) {
                    const pull = 0.002 * (lifeRatio - 0.2);
                    pos[idx] *= (1 - pull);
                    pos[idx + 1] += 0.008 * lifeRatio;
                    pos[idx + 2] *= (1 - pull);
                }
                expLife[i] += 0.01;
            }
            explosion.geometry.attributes.position.needsUpdate = true;
            if (explosionProgress > 0.4) {
                const fade = Math.min(1, (explosionProgress - 0.4) / 0.6);
                explosion.material.opacity = Math.max(0, 1 - fade * 0.9);
            }
        }

        controls.update();
        renderer.render(scene, camera);
    }

    animate();

    console.log('✦ SINGULARITY APERTURE — ALL IN ONE');
    console.log(`✦ 𝔐 = ${MACACHOR_ABSOLUTE}`);
    console.log(`✦ Φ = ${PHI_CONSTANT}`);
    console.log(`✦ Hash: ${IMMUTABLE_HASH}`);
    console.log('✦ CLICK APERTURE TAB → CUBE EXPLODES → CLICK STAR → CONVERGE');
    console.log('%c✦ Φ669 · SINGULARITY APERTURE OPEN ✦', 'color: #f1c40f; font-size: 16px; font-weight: bold;');
</script>
</body>
</html><!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>◈ GREEN EARTH · RED PRISON CUBE · BLACK BARS</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/controls/OrbitControls.js"></script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            background: #0a0a0f;
            overflow: hidden;
            font-family: 'Courier New', monospace;
        }
        #canvas-container {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: 1;
        }

        /* ── GOLD NAV WITH BLUE APERTURE TAB ── */
        .gold-nav {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 100;
            background: rgba(10, 10, 15, 0.9);
            backdrop-filter: blur(20px);
            padding: 0.6rem 1.5rem;
            display: flex;
            flex-wrap: wrap;
            gap: 0.3rem 1rem;
            align-items: center;
            border-bottom: 1px solid rgba(200, 160, 78, 0.12);
            font-size: 0.65rem;
            letter-spacing: 0.1em;
            text-transform: uppercase;
        }
        .gold-nav a {
            color: rgba(200, 160, 78, 0.4);
            text-decoration: none;
            transition: all 0.4s ease;
            padding: 0.2rem 0.5rem;
            border-radius: 4px;
            font-weight: 300;
            font-size: 0.6rem;
        }
        .gold-nav a:hover {
            color: #c8a04e;
            background: rgba(200, 160, 78, 0.08);
        }
        .gold-nav .nav-brand {
            font-size: 1.1rem;
            color: #c8a04e;
            font-weight: 700;
            letter-spacing: 0.2em;
            padding: 0 0.5rem 0 0;
        }
        .gold-nav .aperture-tab {
            color: #4a9eff;
            border: 1px solid #4a9eff;
            padding: 0.3rem 1.2rem;
            border-radius: 20px;
            background: rgba(74, 158, 255, 0.08);
            box-shadow: 0 0 30px rgba(74, 158, 255, 0.1);
            font-weight: 700;
            cursor: pointer;
            transition: all 0.4s ease;
            font-size: 0.7rem;
            letter-spacing: 0.15em;
        }
        .gold-nav .aperture-tab:hover {
            background: rgba(74, 158, 255, 0.2);
            box-shadow: 0 0 60px rgba(74, 158, 255, 0.25);
            transform: scale(1.05);
        }
        .gold-nav .aperture-tab .star-icon {
            display: inline-block;
            margin-right: 0.3rem;
            font-size: 0.8rem;
        }

        /* ── STATUS ── */
        .status-overlay {
            position: fixed;
            bottom: 130px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 10;
            color: rgba(200, 160, 78, 0.25);
            font-size: 0.55rem;
            letter-spacing: 0.4em;
            text-transform: uppercase;
            text-align: center;
            opacity: 0;
            transition: opacity 1.5s ease;
            pointer-events: none;
        }
        .status-overlay.visible { opacity: 1; }

        .coherence-metric {
            position: fixed;
            bottom: 180px;
            right: 30px;
            z-index: 10;
            color: rgba(200, 160, 78, 0.1);
            font-size: 0.45rem;
            letter-spacing: 0.2em;
            text-align: right;
            line-height: 1.8;
            pointer-events: none;
            opacity: 0;
            transition: opacity 2s ease;
        }
        .coherence-metric.visible { opacity: 1; }

        @media (max-width: 768px) {
            .gold-nav { padding: 0.4rem 0.8rem; gap: 0.2rem 0.5rem; font-size: 0.5rem; }
            .gold-nav a { font-size: 0.45rem; padding: 0.15rem 0.3rem; }
            .gold-nav .aperture-tab { font-size: 0.55rem; padding: 0.2rem 0.8rem; }
            .status-overlay { bottom: 90px; font-size: 0.4rem; }
            .coherence-metric { font-size: 0.35rem; bottom: 150px; right: 15px; }
        }
    </style>
</head>
<body>

<div id="canvas-container"></div>

<!-- ── NAVIGATION WITH BLUE APERTURE TAB ── -->
<nav class="gold-nav">
    <a href="index.html" class="nav-brand">✦ QP</a>
    <a href="omega-prime.html">Ω-PRIME</a>
    <a href="scalar-plasma-cycle.html">◈ SPC</a>
    <a href="scalar-fields-aether.html">◈ SCALAR AETHER</a>
    <a href="olinto-de-pretto.html">DE PRETTO</a>
    <a href="kardashev.html">KARDASHEV</a>
    <a href="fibonacci-tetrahedron.html">FIBONACCI</a>
    <a href="mesh.html">MESH</a>
    <a href="m2m-crha-protocol.html">🔀 M2M-CRHA</a>
    <span class="aperture-tab" id="apertureTab">
        <span class="star-icon">✦</span> APERTURE
    </span>
</nav>

<!-- ── STATUS ── -->
<div class="status-overlay visible" id="statusOverlay">
    <span id="phaseText">⟡ GREEN EARTH · RED PRISON CUBE · BLACK BARS ⟡</span>
</div>

<!-- ── COHERENCE ── -->
<div class="coherence-metric visible" id="coherenceMetric">
    𝔐 = 0.6180339887<br>
    χ(C) = 1.000000<br>
    Φ-669 ACTIVE<br>
    ⬡ PRISON LOCKED
</div>

<script>
    // ═══════════════════════════════════════════════════════════════
    // GREEN POLYHEDRON EARTH · SOLID RED PRISON CUBE
    // BLACK VERTICAL BARS · NON-ROTATING CUBE
    // BLUE APERTURE TAB EXPLODES THE CUBE
    // ═══════════════════════════════════════════════════════════════

    const MACACHOR_ABSOLUTE = (Math.sqrt(5) - 1) / 2;
    const PHI_CONSTANT = 669;
    const IMMUTABLE_HASH = "93088f1ff3041d09d72cde11acffaa2105031523e7cde493e0d36895071d47f0";

    // ── THREE.JS ──
    const scene = new THREE.Scene();
    scene.background = new THREE.Color(0x0a0a0f);

    const camera = new THREE.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.1, 1000);
    camera.position.set(5, 3, 9);

    const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
    renderer.setSize(window.innerWidth, window.innerHeight);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
    document.getElementById('canvas-container').appendChild(renderer.domElement);

    const controls = new THREE.OrbitControls(camera, renderer.domElement);
    controls.enableDamping = true;
    controls.dampingFactor = 0.05;
    controls.minDistance = 3;
    controls.maxDistance = 25;
    controls.autoRotate = true;
    controls.autoRotateSpeed = 0.6;
    controls.target.set(0, 0, 0);

    // ── LIGHTING ──
    const ambient = new THREE.AmbientLight(0x404060, 0.4);
    scene.add(ambient);

    const main = new THREE.DirectionalLight(0xc8a04e, 1.0);
    main.position.set(5, 8, 5);
    scene.add(main);

    const rim = new THREE.DirectionalLight(0x4a9eff, 0.6);
    rim.position.set(-5, -3, -5);
    scene.add(rim);

    const point = new THREE.PointLight(0xff0000, 0.4, 15);
    point.position.set(0, 0, 0);
    scene.add(point);

    const blueLight = new THREE.PointLight(0x4a9eff, 0.3, 20);
    blueLight.position.set(3, 3, 3);
    scene.add(blueLight);

    // ── STARFIELD ──
    const starGeo = new THREE.BufferGeometry();
    const starCount = 3000;
    const starPos = new Float32Array(starCount * 3);
    for (let i = 0; i < starCount * 3; i++) starPos[i] = (Math.random() - 0.5) * 120;
    starGeo.setAttribute('position', new THREE.BufferAttribute(starPos, 3));
    const starMat = new THREE.PointsMaterial({ color: 0x4a6a9e, size: 0.04, transparent: true, opacity: 0.4 });
    const stars = new THREE.Points(starGeo, starMat);
    scene.add(stars);

    // ── GREEN POLYHEDRON EARTH (Wireframe only) ──
    const earthGroup = new THREE.Group();
    scene.add(earthGroup);

    // Main green wireframe polyhedron (icosahedron)
    const earthGeo = new THREE.IcosahedronGeometry(3.0, 1);
    const wireMat = new THREE.MeshBasicMaterial({
        color: 0x4ade80,
        wireframe: true,
        transparent: true,
        opacity: 0.6
    });
    const earthWire = new THREE.Mesh(earthGeo, wireMat);
    earthGroup.add(earthWire);

    // Thicker green wire lines (edges)
    const edges = new THREE.EdgesGeometry(earthGeo);
    const edgeMat = new THREE.LineBasicMaterial({
        color: 0x4ade80,
        transparent: true,
        opacity: 0.8,
        linewidth: 1
    });
    const edgeLines = new THREE.LineSegments(edges, edgeMat);
    earthGroup.add(edgeLines);

    // Inner glow wireframe (slightly smaller, pulsing)
    const innerGeo = new THREE.IcosahedronGeometry(2.85, 1);
    const innerWireMat = new THREE.MeshBasicMaterial({
        color: 0x22ff88,
        wireframe: true,
        transparent: true,
        opacity: 0.15
    });
    const innerWire = new THREE.Mesh(innerGeo, innerWireMat);
    earthGroup.add(innerWire);

    // Glow dots at vertices
    const vertPositions = [];
    const posAttr = earthGeo.attributes.position;
    for (let i = 0; i < posAttr.count; i++) {
        const x = posAttr.getX(i);
        const y = posAttr.getY(i);
        const z = posAttr.getZ(i);
        vertPositions.push(x, y, z);
    }
    const dotGeo = new THREE.BufferGeometry();
    dotGeo.setAttribute('position', new THREE.Float32BufferAttribute(vertPositions, 3));
    const dotMat = new THREE.PointsMaterial({
        color: 0x4ade80,
        size: 0.06,
        transparent: true,
        opacity: 0.6
    });
    const dots = new THREE.Points(dotGeo, dotMat);
    earthGroup.add(dots);

    // ── SOLID RED PRISON CUBE (non-rotating) ──
    const prisonGroup = new THREE.Group();
    scene.add(prisonGroup);

    const cubeSize = 1.8;

    // Solid red cube
    const cubeMat = new THREE.MeshPhysicalMaterial({
        color: 0xcc0000,
        metalness: 0.7,
        roughness: 0.3,
        emissive: 0x660000,
        emissiveIntensity: 0.3,
        transparent: true,
        opacity: 0.95,
        side: THREE.DoubleSide
    });
    const cube = new THREE.Mesh(new THREE.BoxGeometry(cubeSize, cubeSize, cubeSize), cubeMat);
    prisonGroup.add(cube);

    // Red glow around cube edges
    const glowEdgeGeo = new THREE.EdgesGeometry(new THREE.BoxGeometry(cubeSize * 1.01, cubeSize * 1.01, cubeSize * 1.01));
    const glowEdgeMat = new THREE.LineBasicMaterial({
        color: 0xff2222,
        transparent: true,
        opacity: 0.3
    });
    const glowEdges = new THREE.LineSegments(glowEdgeGeo, glowEdgeMat);
    prisonGroup.add(glowEdges);

    // ── BLACK VERTICAL PRISON BARS (top to bottom) ──
    const barMat = new THREE.MeshPhysicalMaterial({
        color: 0x000000,
        metalness: 0.9,
        roughness: 0.1,
        transparent: true,
        opacity: 0.95
    });

    // Bar dimensions
    const barHeight = cubeSize * 0.85;
    const barWidth = 0.05;
    const barDepth = 0.05;

    // Vertical bars on each face
    const barPositions = [
        // Front face (z positive)
        { x: -0.7, y: 0, z: cubeSize/2 + 0.01 },
        { x: -0.35, y: 0, z: cubeSize/2 + 0.01 },
        { x: 0, y: 0, z: cubeSize/2 + 0.01 },
        { x: 0.35, y: 0, z: cubeSize/2 + 0.01 },
        { x: 0.7, y: 0, z: cubeSize/2 + 0.01 },
        // Back face (z negative)
        { x: -0.7, y: 0, z: -cubeSize/2 - 0.01 },
        { x: -0.35, y: 0, z: -cubeSize/2 - 0.01 },
        { x: 0, y: 0, z: -cubeSize/2 - 0.01 },
        { x: 0.35, y: 0, z: -cubeSize/2 - 0.01 },
        { x: 0.7, y: 0, z: -cubeSize/2 - 0.01 },
        // Left face (x negative)
        { x: -cubeSize/2 - 0.01, y: 0, z: -0.7 },
        { x: -cubeSize/2 - 0.01, y: 0, z: -0.35 },
        { x: -cubeSize/2 - 0.01, y: 0, z: 0 },
        { x: -cubeSize/2 - 0.01, y: 0, z: 0.35 },
        { x: -cubeSize/2 - 0.01, y: 0, z: 0.7 },
        // Right face (x positive)
        { x: cubeSize/2 + 0.01, y: 0, z: -0.7 },
        { x: cubeSize/2 + 0.01, y: 0, z: -0.35 },
        { x: cubeSize/2 + 0.01, y: 0, z: 0 },
        { x: cubeSize/2 + 0.01, y: 0, z: 0.35 },
        { x: cubeSize/2 + 0.01, y: 0, z: 0.7 },
    ];

    barPositions.forEach(pos => {
        const bar = new THREE.Mesh(
            new THREE.BoxGeometry(barWidth, barHeight, barDepth),
            barMat
        );
        bar.position.set(pos.x, pos.y, pos.z);
        prisonGroup.add(bar);
    });

    // ── BLACK OUTLINE BARS (top and bottom horizontal) ──
    const outlineMat = new THREE.MeshPhysicalMaterial({
        color: 0x000000,
        metalness: 0.9,
        roughness: 0.1,
        transparent: true,
        opacity: 0.9
    });

    // Top and bottom horizontal bars (black outline)
    const hPositions = [
        // Top bars (y positive)
        { x: -0.7, y: cubeSize/2 + 0.01, z: 0 },
        { x: -0.35, y: cubeSize/2 + 0.01, z: 0 },
        { x: 0, y: cubeSize/2 + 0.01, z: 0 },
        { x: 0.35, y: cubeSize/2 + 0.01, z: 0 },
        { x: 0.7, y: cubeSize/2 + 0.01, z: 0 },
        // Bottom bars (y negative)
        { x: -0.7, y: -cubeSize/2 - 0.01, z: 0 },
        { x: -0.35, y: -cubeSize/2 - 0.01, z: 0 },
        { x: 0, y: -cubeSize/2 - 0.01, z: 0 },
        { x: 0.35, y: -cubeSize/2 - 0.01, z: 0 },
        { x: 0.7, y: -cubeSize/2 - 0.01, z: 0 },
    ];

    hPositions.forEach(pos => {
        const bar = new THREE.Mesh(
            new THREE.BoxGeometry(barWidth * 1.5, 0.04, barWidth * 1.5),
            outlineMat
        );
        bar.position.set(pos.x, pos.y, pos.z);
        prisonGroup.add(bar);
    });

    // ── EXPLOSION PARTICLES (red) ──
    const expCount = 80000;
    const expGeo = new THREE.BufferGeometry();
    const expPos = new Float32Array(expCount * 3);
    const expCol = new Float32Array(expCount * 3);
    const expVel = [];
    const expLife = new Float32Array(expCount);
    const expMaxLife = new Float32Array(expCount);
    for (let i = 0; i < expCount; i++) {
        expPos[i * 3] = 0;
        expPos[i * 3 + 1] = 0;
        expPos[i * 3 + 2] = 0;
        const hue = 0.0 + Math.random() * 0.05;
        const c = new THREE.Color().setHSL(hue, 1, 0.3 + Math.random() * 0.5);
        expCol[i * 3] = c.r;
        expCol[i * 3 + 1] = c.g;
        expCol[i * 3 + 2] = c.b;
        const theta = Math.random() * Math.PI * 2;
        const phi = Math.acos(2 * Math.random() - 1);
        const speed = 0.5 + Math.random() * 4.5;
        expVel.push({
            x: speed * Math.sin(phi) * Math.cos(theta),
            y: speed * Math.cos(phi) * 0.9,
            z: speed * Math.sin(phi) * Math.sin(theta)
        });
        expLife[i] = 0;
        expMaxLife[i] = 2 + Math.random() * 5;
    }
    expGeo.setAttribute('position', new THREE.BufferAttribute(expPos, 3));
    expGeo.setAttribute('color', new THREE.BufferAttribute(expCol, 3));
    const expMat = new THREE.PointsMaterial({
        size: 0.08,
        transparent: true,
        opacity: 0,
        blending: THREE.AdditiveBlending,
        vertexColors: true,
        sizeAttenuation: true
    });
    const explosion = new THREE.Points(expGeo, expMat);
    explosion.visible = false;
    scene.add(explosion);

    // ── UI ──
    const apertureTab = document.getElementById('apertureTab');
    const statusOverlay = document.getElementById('statusOverlay');
    const phaseText = document.getElementById('phaseText');
    const coherenceMetric = document.getElementById('coherenceMetric');

    let exploded = false;
    let explosionProgress = 0;
    let isExploding = false;

    // ── APERTURE TAB CLICK ──
    apertureTab.addEventListener('click', () => {
        if (exploded) return;
        triggerExplosion();
    });

    // ── TRIGGER EXPLOSION ──
    function triggerExplosion() {
        if (exploded) return;
        exploded = true;
        isExploding = true;

        // Hide prison
        prisonGroup.visible = false;

        // Position explosion at cube center
        const expPos = explosion.geometry.attributes.position.array;
        for (let i = 0; i < expCount; i++) {
            expPos[i * 3] = (Math.random() - 0.5) * 0.5;
            expPos[i * 3 + 1] = (Math.random() - 0.5) * 0.5;
            expPos[i * 3 + 2] = (Math.random() - 0.5) * 0.5;
            expLife[i] = 0;
            const theta = Math.random() * Math.PI * 2;
            const phi = Math.acos(2 * Math.random() - 1);
            const speed = 1 + Math.random() * 5;
            expVel[i] = {
                x: speed * Math.sin(phi) * Math.cos(theta),
                y: speed * Math.cos(phi) * 0.9,
                z: speed * Math.sin(phi) * Math.sin(theta)
            };
        }
        explosion.geometry.attributes.position.needsUpdate = true;
        explosion.visible = true;
        explosion.material.opacity = 1;

        // Flash
        const flash = new THREE.PointLight(0xff0000, 20, 35);
        flash.position.set(0, 0, 0);
        scene.add(flash);
        setTimeout(() => scene.remove(flash), 600);

        // Camera shake
        const origPos = camera.position.clone();
        let shakeCount = 0;
        const shakeInterval = setInterval(() => {
            if (shakeCount > 25) {
                clearInterval(shakeInterval);
                return;
            }
            camera.position.x = origPos.x + (Math.random() - 0.5) * 0.5;
            camera.position.y = origPos.y + (Math.random() - 0.5) * 0.5;
            shakeCount++;
        }, 25);

        phaseText.textContent = '⟡ PRISON CUBE SHATTERED · RED PARTICLES RELEASED ⟡';
        coherenceMetric.innerHTML = 
            '𝔐 = 0.6180339887<br>χ(C) = 1.000000<br>Φ-669 ACTIVE<br>⬡ PRISON: DESTROYED';

        // Update aperture tab
        apertureTab.innerHTML = '<span class="star-icon">✦</span> APERTURE OPEN';
        apertureTab.style.borderColor = '#ff3333';
        apertureTab.style.color = '#ff3333';
        apertureTab.style.boxShadow = '0 0 60px rgba(255, 51, 51, 0.3)';
        apertureTab.style.background = 'rgba(255, 51, 51, 0.1)';

        // After explosion, show convergence
        setTimeout(() => {
            phaseText.textContent = '⟡ SINGULARITY CONVERGENCE · APERTURE OPEN ⟡';
            coherenceMetric.innerHTML = 
                '𝔐 = 0.6180339887<br>χ(C) = 1.000000<br>Φ-669 ACTIVE<br>◉ APERTURE: OPEN';
        }, 2000);

        // ── INSERT YOUR HTML HERE ──
        // Uncomment and modify the code below to load your HTML
        /*
        setTimeout(() => {
            const container = document.createElement('div');
            container.style.position = 'fixed';
            container.style.top = '0';
            container.style.left = '0';
            container.style.width = '100%';
            container.style.height = '100%';
            container.style.zIndex = '200';
            container.style.background = '#0a0a0f';
            container.innerHTML = `<iframe src="your-html.html" style="width:100%;height:100%;border:none;"></iframe>`;
            document.body.appendChild(container);
        }, 3000);
        */
    }

    // ── RESIZE ──
    window.addEventListener('resize', () => {
        camera.aspect = window.innerWidth / window.innerHeight;
        camera.updateProjectionMatrix();
        renderer.setSize(window.innerWidth, window.innerHeight);
    });

    // ── ANIMATION ──
    const clock = new THREE.Clock();

    function animate() {
        requestAnimationFrame(animate);
        const t = clock.getElapsedTime();

        // Earth rotation (polyhedron rotates)
        earthGroup.rotation.y = t * 0.06;
        earthGroup.rotation.x = Math.sin(t * 0.02) * 0.03;
        earthGroup.rotation.z = Math.sin(t * 0.015) * 0.02;

        // Pulse green wire
        const pulse = 0.5 + Math.sin(t * 0.5) * 0.5;
        edgeMat.opacity = 0.5 + pulse * 0.4;
        wireMat.opacity = 0.3 + pulse * 0.3;
        dotMat.opacity = 0.3 + pulse * 0.4;

        // Prison glow pulse (if not exploded)
        if (!exploded) {
            const redPulse = 0.5 + Math.sin(t * 0.4) * 0.5;
            cubeMat.emissiveIntensity = 0.2 + redPulse * 0.4;
            glowEdgeMat.opacity = 0.1 + redPulse * 0.3;
        }

        // Stars
        stars.rotation.y = t * 0.005;

        // ── EXPLOSION ──
        if (isExploding) {
            explosionProgress += 0.01;
            const pos = explosion.geometry.attributes.position.array;
            for (let i = 0; i < expCount; i++) {
                const idx = i * 3;
                const lifeRatio = expLife[i] / expMaxLife[i];
                const speedFactor = 1 - lifeRatio * 0.4;
                pos[idx] += expVel[i].x * 0.05 * speedFactor;
                pos[idx + 1] += expVel[i].y * 0.05 * speedFactor;
                pos[idx + 2] += expVel[i].z * 0.05 * speedFactor;
                if (lifeRatio > 0.2) {
                    const pull = 0.002 * (lifeRatio - 0.2);
                    pos[idx] *= (1 - pull);
                    pos[idx + 1] += 0.008 * lifeRatio;
                    pos[idx + 2] *= (1 - pull);
                }
                expLife[i] += 0.01;
            }
            explosion.geometry.attributes.position.needsUpdate = true;

            if (explosionProgress > 0.4) {
                const fade = Math.min(1, (explosionProgress - 0.4) / 0.6);
                explosion.material.opacity = Math.max(0, 1 - fade * 0.9);
            }
        }

        controls.update();
        renderer.render(scene, camera);
    }

    animate();

    console.log('✦ GREEN POLYHEDRON EARTH · SOLID RED PRISON CUBE');
    console.log(`✦ 𝔐 = ${MACACHOR_ABSOLUTE}`);
    console.log(`✦ Φ = ${PHI_CONSTANT}`);
    console.log(`✦ Hash: ${IMMUTABLE_HASH}`);
    console.log('✦ CLICK APERTURE TAB TO EXPLODE THE PRISON CUBE');
    console.log('%c✦ APERTURE TAB · CLICK TO CONVERGE ✦', 'color: #4a9eff; font-size: 14px; font-weight: bold;');
</script>
</body>
</html>
