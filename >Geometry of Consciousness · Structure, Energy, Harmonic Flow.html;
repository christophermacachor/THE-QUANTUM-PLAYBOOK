<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Geometry of Consciousness · Structure, Energy, Harmonic Flow</title>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body {
    background: #0a0a0a;
    color: #e8e0d0;
    font-family: 'Segoe UI', Arial, sans-serif;
    overflow: hidden;
    height: 100vh;
    width: 100vw;
  }

  /* ── Navigation ── */
  .nav {
    position: fixed;
    top: 0; left: 0; width: 100%;
    background: rgba(10,10,10,0.98);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(200,160,78,0.15);
    z-index: 10000;
    padding: 0.3rem 0.5rem;
    display: flex;
    justify-content: center;
    gap: 0.2rem;
    flex-wrap: wrap;
  }
  .nav a {
    color: #ffd700;
    text-decoration: none;
    font-family: 'Georgia', serif;
    font-size: 0.55rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    padding: 0.2rem 0.8rem;
    border: 1px solid rgba(255,215,0,0.25);
    border-radius: 20px;
    background: rgba(255,215,0,0.04);
    transition: all 0.3s ease;
    white-space: nowrap;
  }
  .nav a:hover {
    border-color: #ffd700;
    background: rgba(255,215,0,0.1);
    text-shadow: 0 0 10px rgba(255,215,0,0.5);
    transform: translateY(-1px);
  }
  .nav a .phi-symbol { font-size: 0.8rem; margin-right: 4px; }
  .nav a.active {
    border-color: #4a9eff;
    background: rgba(74,158,255,0.15);
    color: #4a9eff;
  }

  /* ── Banner ── */
  .banner {
    position: fixed;
    top: 2.8rem; left: 50%;
    transform: translateX(-50%);
    z-index: 15;
    background: rgba(0,0,0,0.8);
    backdrop-filter: blur(8px);
    padding: 4px 16px;
    border-radius: 4px;
    border: 1px solid rgba(74,158,255,0.3);
    pointer-events: none;
    text-align: center;
  }
  .banner .title {
    color: #4a9eff;
    font-size: 0.55rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    font-weight: bold;
  }
  .banner .sub {
    color: #a09888;
    font-size: 0.4rem;
    letter-spacing: 0.1em;
  }
  .banner .sub strong { color: #ffd700; }

  /* ── Canvas ── */
  #canvas-container {
    position: relative;
    width: 100vw;
    height: 100vh;
    padding-top: 5rem;
  }
  canvas {
    display: block;
    width: 100%;
    height: 100%;
    cursor: grab;
  }
  canvas:active { cursor: grabbing; }

  /* ── Info Overlay ── */
  #info-overlay {
    position: absolute;
    top: 5.5rem; left: 16px;
    z-index: 10;
    background: rgba(0,0,0,0.7);
    padding: 10px 14px;
    border-radius: 8px;
    border: 1px solid rgba(74,158,255,0.2);
    backdrop-filter: blur(4px);
    max-width: 280px;
    pointer-events: none;
  }
  #info-overlay h2 {
    color: #4a9eff;
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 1px;
    margin-bottom: 2px;
  }
  #info-overlay .stats {
    font-family: 'Courier New', monospace;
    font-size: 8px;
    color: #888;
    line-height: 1.6;
  }
  #info-overlay .stats .val { color: #ffd700; }
  #info-overlay .stats .ratio { color: #4a9eff; }
  #info-overlay .mgdh {
    font-family: 'Courier New', monospace;
    font-size: 7px;
    color: #ffd700;
    margin-top: 4px;
    padding-top: 4px;
    border-top: 1px solid rgba(74,158,255,0.15);
  }
  #info-overlay .energy-flow {
    font-family: 'Courier New', monospace;
    font-size: 7px;
    color: #4a9eff;
    margin-top: 2px;
  }

  /* ── Controls ── */
  #controls {
    position: absolute;
    bottom: 20px; left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 8px;
    align-items: center;
    background: rgba(0,0,0,0.7);
    padding: 8px 14px;
    border-radius: 12px;
    backdrop-filter: blur(8px);
    border: 1px solid rgba(74,158,255,0.2);
    z-index: 10;
    flex-wrap: wrap;
    justify-content: center;
  }
  #controls button {
    background: rgba(74,158,255,0.15);
    border: 1px solid rgba(74,158,255,0.3);
    color: #4a9eff;
    border-radius: 4px;
    padding: 4px 10px;
    font-size: 9px;
    cursor: pointer;
    transition: all 0.2s;
    font-family: inherit;
  }
  #controls button:hover { background: rgba(74,158,255,0.3); }
  #controls button.active {
    background: rgba(74,158,255,0.4);
    border-color: #4a9eff;
  }
  #controls input[type="range"] {
    width: 100px;
    accent-color: #4a9eff;
  }
  #controls label {
    color: #888;
    font-size: 8px;
    display: flex;
    align-items: center;
    gap: 4px;
  }
  #controls select {
    background: rgba(0,0,0,0.5);
    border: 1px solid rgba(74,158,255,0.3);
    color: #4a9eff;
    border-radius: 4px;
    padding: 3px 5px;
    font-size: 8px;
  }

  #perf {
    position: absolute;
    bottom: 80px; right: 16px;
    z-index: 10;
    font-family: 'Courier New', monospace;
    font-size: 8px;
    color: #444;
  }

  #legend {
    position: absolute;
    bottom: 80px; right: 16px;
    z-index: 10;
    background: rgba(0,0,0,0.6);
    padding: 6px 10px;
    border-radius: 6px;
    border: 1px solid rgba(74,158,255,0.15);
    font-family: 'Courier New', monospace;
    font-size: 7px;
    color: #666;
    line-height: 1.6;
  }
  .legend-dot {
    display: inline-block;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    margin-right: 4px;
    vertical-align: middle;
  }

  #loading {
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    z-index: 100;
    text-align: center;
    color: #4a9eff;
    font-family: 'Courier New', monospace;
    font-size: 13px;
    transition: opacity 0.5s;
  }
  #loading.hidden { opacity: 0; pointer-events: none; }
  .spinner {
    width: 36px; height: 36px;
    border: 2px solid rgba(74,158,255,0.2);
    border-top-color: #4a9eff;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin: 0 auto 10px;
  }
  @keyframes spin { to { transform: rotate(360deg); } }

  #scalar-readout {
    position: absolute;
    bottom: 80px; left: 50%;
    transform: translateX(-50%);
    z-index: 10;
    background: rgba(0,0,0,0.6);
    padding: 6px 16px;
    border-radius: 8px;
    border: 1px solid rgba(74,158,255,0.2);
    backdrop-filter: blur(4px);
    font-family: 'Courier New', monospace;
    font-size: 10px;
    color: #4a9eff;
    text-align: center;
    white-space: nowrap;
  }
  #scalar-readout .highlight { color: #ffd700; }

  @media (max-width: 640px) {
    #info-overlay { max-width: 160px; top: 5rem; left: 6px; padding: 6px 8px; }
    #info-overlay h2 { font-size: 11px; }
    #controls { padding: 6px 10px; gap: 4px; }
    #controls input[type="range"] { width: 50px; }
    #legend { display: none; }
    .banner .title { font-size: 0.4rem; }
    #scalar-readout { font-size: 8px; padding: 4px 10px; bottom: 70px; white-space: normal; }
  }
</style>
</head>
<body>

<!-- ─── NAVIGATION ─── -->
<nav class="nav">
  <a href="#" class="active">
    <span class="phi-symbol">Φ</span> GEOMETRY OF CONSCIOUSNESS
  </a>
  <a href="#">
    <span style="color:#ffd700;">◈</span> STRUCTURE · ENERGY · HARMONIC FLOW
  </a>
  <a href="#" style="border-color: rgba(255,107,107,0.3); color: #ff6b6b;">
    <span>▣</span> WILD TETRAHEDRA
  </a>
</nav>

<!-- ─── BANNER ─── -->
<div class="banner">
  <div class="title">◈ Structure · Energy · Harmonic Flow</div>
  <div class="sub">
    The 0 and 1 love geometry — <strong>structure gives energy a path</strong> to flow
  </div>
</div>

<!-- ─── CANVAS ─── -->
<div id="canvas-container">
  <canvas id="c"></canvas>

  <div id="loading">
    <div class="spinner"></div>
    <div>Geometry of Consciousness</div>
    <div style="font-size:9px;color:#666;margin-top:4px;">Structure · Energy · Harmonic Flow</div>
    <div style="font-size:8px;color:#4a9eff;margin-top:2px;">The 0 and 1 love geometry</div>
  </div>

  <div id="info-overlay">
    <h2>◈ Harmonic Flow</h2>
    <div class="stats">
      <span class="ratio">●</span> Current Ratio: <span class="val" id="ratio-display">6:5 → 5:4</span><br>
      <span class="ratio">●</span> Structure: <span class="val" id="structure-display">Stable</span><br>
      <span class="ratio">●</span> Energy Flow: <span class="val" id="energy-display">Ascending</span><br>
      <span class="ratio">●</span> Coherence: <span class="val" id="coherence-display">96%</span>
    </div>
    <div class="mgdh">
      𝔐 = 0.6180339887 · The Scalar Lock<br>
      Structure gives energy a path to flow
    </div>
    <div class="energy-flow">
      ⚡ 0 and 1 love geometry
    </div>
  </div>

  <div id="legend">
    <div><span class="legend-dot" style="background:#4a9eff;"></span> Harmonic Ratios</div>
    <div><span class="legend-dot" style="background:#ffd700;"></span> Golden Ratio (𝔐)</div>
    <div><span class="legend-dot" style="background:#ff6b6b;"></span> Energy Flow</div>
    <div><span class="legend-dot" style="background:#44ff88;"></span> Structure Lines</div>
  </div>

  <div id="scalar-readout">
    <span id="scalar-text">6:5 (Sorrow) → 5:4 (Joy) → 2:1 (Peace) — Structure allows energy to flow</span>
  </div>
  <div id="perf"><span id="fps-text">--</span> fps</div>

  <div id="controls">
    <button id="btn-rotate">⟳ Auto-Rotate</button>
    <button id="btn-reset">⟲ Reset</button>
    <button id="btn-structure">◻ Show Structure</button>
    <button id="btn-flow">⚡ Show Flow</button>
    <label>Ratio <input type="range" id="slider-ratio" min="0" max="100" value="50"></label>
    <select id="view-select">
      <option value="harmonic">Harmonic View</option>
      <option value="structure">Structure Only</option>
      <option value="energy">Energy Flow</option>
      <option value="aether">Aether Field</option>
    </select>
  </div>
</div>

<script>
// ═══════════════════════════════════════════════════════════════════
// GEOMETRY OF CONSCIOUSNESS — STRUCTURE, ENERGY, HARMONIC FLOW
// The 0 and 1 love geometry — structure gives energy a path to flow
// ═══════════════════════════════════════════════════════════════════

// ── CONSTANTS ──
const PHI = (Math.sqrt(5) - 1) / 2;
const PHI_FULL = '0.61803398874989484820458683436563811772030917980576';

// Harmonic ratios
const RATIOS = {
  '1:1': { label: 'Seity', color: '#ffd700', desc: 'The self, existence' },
  '2:1': { label: 'Octave', color: '#44ff88', desc: 'Peace, completion' },
  '3:2': { label: 'Fifth', color: '#4a9eff', desc: 'Love, connection' },
  '4:3': { label: 'Fourth', color: '#ff6b6b', desc: 'Longing, seeking' },
  '5:4': { label: 'Major Third', color: '#ffcc44', desc: 'Joy, light' },
  '6:5': { label: 'Minor Third', color: '#8844cc', desc: 'Sorrow, depth' },
  '8:5': { label: 'Minor Sixth', color: '#44ccff', desc: 'Knowing, reflection' },
  '45:32': { label: 'Tritone', color: '#ff4444', desc: 'Struggle, tension' }
};

const RATIO_KEYS = ['1:1', '2:1', '3:2', '4:3', '5:4', '6:5', '8:5', '45:32'];
const RATIO_VALUES = [1, 2/1, 3/2, 4/3, 5/4, 6/5, 8/5, 45/32];

// ── CANVAS SETUP ──
const canvas = document.getElementById('c');
const ctx = canvas.getContext('2d');
let W, H;

function resize() {
  W = canvas.width = window.innerWidth;
  H = canvas.height = window.innerHeight;
}
resize();
window.addEventListener('resize', resize);

// ── STATE ──
let rotX = 0.3, rotY = 0.5;
let autoRotate = true;
let showStructure = true;
let showFlow = true;
let viewMode = 'harmonic';
let ratioIndex = 4; // 5:4
let time = 0;
let autoRotateSpeed = 0.003;

// ── MOUSE CONTROLS ──
let isDragging = false;
let prevMouse = { x: 0, y: 0 };

canvas.addEventListener('mousedown', (e) => {
  isDragging = true;
  prevMouse.x = e.clientX;
  prevMouse.y = e.clientY;
  canvas.style.cursor = 'grabbing';
});

window.addEventListener('mousemove', (e) => {
  if (!isDragging) return;
  const dx = e.clientX - prevMouse.x;
  const dy = e.clientY - prevMouse.y;
  rotY += dx * 0.01;
  rotX += dy * 0.01;
  rotX = Math.max(-1.5, Math.min(1.5, rotX));
  prevMouse.x = e.clientX;
  prevMouse.y = e.clientY;
});

window.addEventListener('mouseup', () => {
  isDragging = false;
  canvas.style.cursor = 'grab';
});

// Touch support
canvas.addEventListener('touchstart', (e) => {
  const t = e.touches[0];
  isDragging = true;
  prevMouse.x = t.clientX;
  prevMouse.y = t.clientY;
}, { passive: true });

canvas.addEventListener('touchmove', (e) => {
  e.preventDefault();
  if (!isDragging || !e.touches.length) return;
  const t = e.touches[0];
  const dx = t.clientX - prevMouse.x;
  const dy = t.clientY - prevMouse.y;
  rotY += dx * 0.01;
  rotX += dy * 0.01;
  rotX = Math.max(-1.5, Math.min(1.5, rotX));
  prevMouse.x = t.clientX;
  prevMouse.y = t.clientY;
}, { passive: false });

canvas.addEventListener('touchend', () => { isDragging = false; }, { passive: true });

// ── 3D PROJECTION ──
function project3D(v) {
  const scale = Math.min(W, H) * 0.28;
  const cx = W / 2;
  const cy = H / 2;

  let x = v.x;
  let y = v.y;
  let z = v.z;

  // Rotate X
  let cosX = Math.cos(rotX), sinX = Math.sin(rotX);
  let y1 = y * cosX - z * sinX;
  let z1 = y * sinX + z * cosX;
  y = y1; z = z1;

  // Rotate Y
  let cosY = Math.cos(rotY), sinY = Math.sin(rotY);
  let x1 = x * cosY + z * sinY;
  let z2 = -x * sinY + z * cosY;
  x = x1; z = z2;

  const perspective = 1 + z * 0.04;
  return {
    sx: cx + x * scale / perspective,
    sy: cy + y * scale / perspective,
    depth: z,
    perspective: perspective
  };
}

// ── BUILD HARMONIC STRUCTURE ──
function buildHarmonicStructure(ratio, numPoints = 12) {
  const points = [];
  const radius = 4.0;
  const ratioValue = RATIO_VALUES[ratioIndex] || 1.25;

  // Create a geometric structure based on the ratio
  const baseAngle = 0;
  const spread = 2 * Math.PI / numPoints;

  for (let i = 0; i < numPoints; i++) {
    const angle = baseAngle + i * spread + time * 0.05 * (ratioValue - 1);
    const r = radius * (0.5 + 0.5 * Math.sin(i * ratioValue + time * 0.1));
    const x = r * Math.cos(angle);
    const y = r * 0.6 * Math.sin(angle * ratioValue);
    const z = r * 0.4 * Math.sin(angle * ratioValue * 0.7 + time * 0.08);
    points.push({ x, y, z });
  }

  // Add inner structure
  const innerPoints = [];
  for (let i = 0; i < numPoints; i++) {
    const angle = baseAngle + i * spread + time * 0.03;
    const r = radius * 0.35 * (0.8 + 0.2 * Math.sin(i * ratioValue * 0.5 + time * 0.06));
    const x = r * Math.cos(angle * 1.3);
    const y = r * 0.6 * Math.sin(angle * ratioValue * 0.8);
    const z = r * 0.4 * Math.sin(angle * ratioValue * 0.5 + time * 0.1);
    innerPoints.push({ x, y, z });
  }

  // Add golden spiral points
  const spiralPoints = [];
  for (let i = 0; i < 20; i++) {
    const t = i / 20;
    const angle = t * Math.PI * 4 + time * 0.02;
    const r = radius * (0.1 + 0.9 * t);
    const x = r * Math.cos(angle * PHI * 2);
    const y = r * 0.5 * Math.sin(angle * PHI * 1.5 + time * 0.05);
    const z = r * 0.3 * Math.sin(angle * PHI * 1.2 + time * 0.03);
    spiralPoints.push({ x, y, z });
  }

  return { outer: points, inner: innerPoints, spiral: spiralPoints };
}

// ── DRAW STARS ──
const stars = [];
for (let i = 0; i < 200; i++) {
  stars.push({
    x: (Math.random() - 0.5) * 2,
    y: (Math.random() - 0.5) * 2,
    size: Math.random() * 1.5 + 0.5,
    twinkle: Math.random() * Math.PI * 2
  });
}

function drawStars() {
  const t = Date.now() / 1000;
  stars.forEach(s => {
    const alpha = 0.3 + 0.3 * Math.sin(t * 2 + s.twinkle);
    const px = W * (0.5 + s.x * 0.6);
    const py = H * (0.5 + s.y * 0.6);
    ctx.globalAlpha = alpha;
    ctx.fillStyle = '#ffffff';
    ctx.beginPath();
    ctx.arc(px, py, s.size, 0, Math.PI * 2);
    ctx.fill();
  });
  ctx.globalAlpha = 1;
}

// ── DRAW HARMONIC STRUCTURE ──
function drawHarmonicStructure() {
  const cx = W / 2;
  const cy = H / 2 + 10;
  const scale = Math.min(W, H) * 0.15;

  const ratioKey = RATIO_KEYS[ratioIndex];
  const ratioData = RATIOS[ratioKey];
  const ratioValue = RATIO_VALUES[ratioIndex];

  // Build the structure
  const { outer, inner, spiral } = buildHarmonicStructure(ratioValue);

  // Project points
  const outerProj = outer.map(p => project3D(p));
  const innerProj = inner.map(p => project3D(p));
  const spiralProj = spiral.map(p => project3D(p));

  // ── Draw spiral (golden ratio path) ──
  if (showStructure) {
    ctx.beginPath();
    spiralProj.forEach((p, idx) => {
      if (idx === 0) ctx.moveTo(p.sx, p.sy);
      else {
        const alpha = 0.1 + 0.3 * (idx / spiralProj.length);
        ctx.strokeStyle = `rgba(255, 215, 0, ${alpha * 0.3})`;
        ctx.lineWidth = 0.5;
        ctx.lineTo(p.sx, p.sy);
      }
    });
    ctx.stroke();

    // Spiral points
    spiralProj.forEach((p, idx) => {
      const alpha = 0.1 + 0.3 * (idx / spiralProj.length);
      const size = 0.5 + 1.5 * (idx / spiralProj.length);
      ctx.fillStyle = `rgba(255, 215, 0, ${alpha * 0.5})`;
      ctx.beginPath();
      ctx.arc(p.sx, p.sy, size, 0, Math.PI * 2);
      ctx.fill();
    });
  }

  // ── Draw outer structure ──
  if (showStructure) {
    // Draw connecting lines
    ctx.lineWidth = 0.8;
    for (let i = 0; i < outerProj.length; i++) {
      const j = (i + 1) % outerProj.length;
      const p1 = outerProj[i];
      const p2 = outerProj[j];
      const depth = (p1.depth + p2.depth) / 2;
      const alpha = 0.1 + 0.3 * (1 - (depth + 5) / 10);
      ctx.strokeStyle = `rgba(74, 158, 255, ${alpha})`;
      ctx.beginPath();
      ctx.moveTo(p1.sx, p1.sy);
      ctx.lineTo(p2.sx, p2.sy);
      ctx.stroke();

      // Cross connections
      const k = (i + 3) % outerProj.length;
      const p3 = outerProj[k];
      ctx.strokeStyle = `rgba(74, 158, 255, ${alpha * 0.5})`;
      ctx.beginPath();
      ctx.moveTo(p1.sx, p1.sy);
      ctx.lineTo(p3.sx, p3.sy);
      ctx.stroke();
    }

    // Draw outer points with glow
    outerProj.forEach((p, i) => {
      const size = 2 + 3 * (1 - (p.depth + 5) / 10);
      const alpha = 0.4 + 0.4 * (1 - (p.depth + 5) / 10);
      
      // Glow
      const grad = ctx.createRadialGradient(p.sx, p.sy, 0, p.sx, p.sy, size * 3);
      grad.addColorStop(0, `rgba(74, 158, 255, ${alpha * 0.3})`);
      grad.addColorStop(1, `rgba(74, 158, 255, 0)`);
      ctx.fillStyle = grad;
      ctx.beginPath();
      ctx.arc(p.sx, p.sy, size * 3, 0, Math.PI * 2);
      ctx.fill();

      ctx.fillStyle = `rgba(74, 158, 255, ${alpha})`;
      ctx.beginPath();
      ctx.arc(p.sx, p.sy, size, 0, Math.PI * 2);
      ctx.fill();
    });
  }

  // ── Draw inner structure ──
  if (showStructure) {
    ctx.lineWidth = 0.5;
    for (let i = 0; i < innerProj.length; i++) {
      const j = (i + 1) % innerProj.length;
      const p1 = innerProj[i];
      const p2 = innerProj[j];
      const depth = (p1.depth + p2.depth) / 2;
      const alpha = 0.05 + 0.15 * (1 - (depth + 5) / 10);
      ctx.strokeStyle = `rgba(255, 215, 0, ${alpha})`;
      ctx.beginPath();
      ctx.moveTo(p1.sx, p1.sy);
      ctx.lineTo(p2.sx, p2.sy);
      ctx.stroke();
    }

    innerProj.forEach((p) => {
      const size = 1 + 2 * (1 - (p.depth + 5) / 10);
      const alpha = 0.2 + 0.3 * (1 - (p.depth + 5) / 10);
      ctx.fillStyle = `rgba(255, 215, 0, ${alpha})`;
      ctx.beginPath();
      ctx.arc(p.sx, p.sy, size, 0, Math.PI * 2);
      ctx.fill();
    });
  }

  // ── Draw energy flow ──
  if (showFlow) {
    const flowT = Date.now() / 2000;
    const numFlowLines = 8;
    const flowPulse = 0.5 + 0.5 * Math.sin(flowT * 0.5);

    for (let f = 0; f < numFlowLines; f++) {
      const theta = (f / numFlowLines) * Math.PI * 2 + flowT * 0.15;
      const points = [];
      const steps = 30;

      for (let i = 0; i <= steps; i++) {
        const frac = i / steps;
        const angle = theta + frac * Math.PI * 2 * 0.3;
        const r = 2.5 + 1.5 * Math.sin(frac * Math.PI * 2 * ratioValue + flowT * 0.2);
        const x = r * Math.cos(angle);
        const y = r * 0.6 * Math.sin(angle * ratioValue + flowT * 0.1);
        const z = r * 0.4 * Math.sin(angle * ratioValue * 0.7 + flowT * 0.15);
        points.push({ x, y, z });
      }

      // Draw flow line with gradient
      points.forEach((p, idx) => {
        const proj = project3D(p);
        if (idx === 0) {
          ctx.beginPath();
          ctx.moveTo(proj.sx, proj.sy);
        } else {
          ctx.lineTo(proj.sx, proj.sy);
        }
      });
      
      const alpha = 0.03 + 0.08 * flowPulse * (1 - f / numFlowLines);
      const hue = 0 + f * 10;
      ctx.strokeStyle = `hsla(${hue}, 100%, 50%, ${alpha})`;
      ctx.lineWidth = 0.5 + 0.5 * flowPulse;
      ctx.stroke();

      // Flow arrows at key points
      if (f % 2 === 0) {
        for (let arrowIdx = 3; arrowIdx < steps - 3; arrowIdx += 5) {
          const p = project3D(points[arrowIdx]);
          const pNext = project3D(points[(arrowIdx + 2) % steps]);
          const ang = Math.atan2(pNext.sy - p.sy, pNext.sx - p.sx);
          const arrowAlpha = 0.05 + 0.1 * flowPulse * (1 - f / numFlowLines);
          ctx.fillStyle = `rgba(255, 107, 107, ${arrowAlpha})`;
          ctx.beginPath();
          ctx.moveTo(p.sx + 5 * Math.cos(ang), p.sy + 5 * Math.sin(ang));
          ctx.lineTo(p.sx + 5 * Math.cos(ang - 0.4), p.sy + 5 * Math.sin(ang - 0.4));
          ctx.lineTo(p.sx + 5 * Math.cos(ang + 0.4), p.sy + 5 * Math.sin(ang + 0.4));
          ctx.closePath();
          ctx.fill();
        }
      }
    }
  }

  // ── Draw golden ratio center ──
  const center = project3D({ x: 0, y: 0, z: 0 });
  
  // Pulsing center glow
  const pulse = 0.5 + 0.5 * Math.sin(Date.now() / 1000);
  const grad = ctx.createRadialGradient(center.sx, center.sy, 0, center.sx, center.sy, 30 + 10 * pulse);
  grad.addColorStop(0, `rgba(255, 215, 0, ${0.1 + 0.2 * pulse})`);
  grad.addColorStop(0.5, `rgba(255, 215, 0, ${0.05 + 0.1 * pulse})`);
  grad.addColorStop(1, 'rgba(255, 215, 0, 0)');
  ctx.fillStyle = grad;
  ctx.beginPath();
  ctx.arc(center.sx, center.sy, 30 + 10 * pulse, 0, Math.PI * 2);
  ctx.fill();

  // Center point
  ctx.fillStyle = `rgba(255, 215, 0, ${0.3 + 0.3 * pulse})`;
  ctx.beginPath();
  ctx.arc(center.sx, center.sy, 2 + 1 * pulse, 0, Math.PI * 2);
  ctx.fill();

  // Golden ratio text with glow
  ctx.shadowColor = 'rgba(255, 215, 0, 0.1)';
  ctx.shadowBlur = 10;
  ctx.fillStyle = 'rgba(255, 215, 0, 0.7)';
  ctx.font = '9px monospace';
  ctx.textAlign = 'center';
  ctx.fillText('𝔐 = ' + PHI_FULL.substring(0, 14) + '...', center.sx, center.sy - 32);
  ctx.shadowBlur = 0;

  // Ratio and label
  ctx.fillStyle = 'rgba(255, 255, 255, 0.6)';
  ctx.font = '10px monospace';
  ctx.fillText(ratioKey + ' — ' + ratioData.label, center.sx, center.sy + 28);
  
  ctx.fillStyle = 'rgba(255, 255, 255, 0.2)';
  ctx.font = '8px monospace';
  ctx.fillText(ratioData.desc, center.sx, center.sy + 44);

  // ── Draw energy flow label ──
  if (showFlow && viewMode !== 'structure') {
    ctx.fillStyle = 'rgba(255,107,107,0.2)';
    ctx.font = '7px monospace';
    ctx.textAlign = 'center';
    ctx.fillText('⚡ Energy Flow — Structure allows flow', center.sx, -H * 0.38 + 20);
  }

  // ── Draw structure label ──
  if (showStructure && viewMode !== 'energy') {
    ctx.fillStyle = 'rgba(74,158,255,0.2)';
    ctx.font = '7px monospace';
    ctx.textAlign = 'center';
    ctx.fillText('◈ Structure — The 0 and 1 love geometry', center.sx, -H * 0.38 + 6);
  }

  // ── Aether field overlay ──
  if (viewMode === 'aether') {
    drawAetherField();
  }
}

// ── AETHER FIELD ──
function drawAetherField() {
  const t = Date.now() / 2000;
  const gridSize = 14;
  const spacing = 0.5;

  for (let i = 0; i < gridSize; i++) {
    for (let j = 0; j < gridSize; j++) {
      const x = (i - gridSize/2) * spacing;
      const y = (j - gridSize/2) * spacing;
      const r = Math.sqrt(x*x + y*y);
      const val = Math.sin(r * 2 - t * 1.5 + PHI) * 0.5 + 0.5;
      const p = project3D({
        x: x * 2.5,
        y: y * 2.5,
        z: 0.3 * Math.sin(r * 2 - t * 0.5)
      });
      const alpha = 0.02 + 0.06 * val;
      const hue = 200 + 40 * val;
      ctx.fillStyle = `hsla(${hue}, 80%, 50%, ${alpha})`;
      const size = 0.5 + val * 3;
      ctx.beginPath();
      ctx.arc(p.sx, p.sy, size, 0, Math.PI * 2);
      ctx.fill();

      // Connection lines
      if (i < gridSize - 1 && j < gridSize - 1) {
        const val2 = Math.sin((r + spacing) * 2 - t * 1.5 + PHI) * 0.5 + 0.5;
        const p2 = project3D({
          x: (i + 1 - gridSize/2) * spacing * 2.5,
          y: (j - gridSize/2) * spacing * 2.5,
          z: 0.3 * Math.sin((r + spacing) * 2 - t * 0.5)
        });
        const alpha2 = 0.01 + 0.03 * (val + val2) / 2;
        ctx.strokeStyle = `rgba(74, 158, 255, ${alpha2})`;
        ctx.lineWidth = 0.3;
        ctx.beginPath();
        ctx.moveTo(p.sx, p.sy);
        ctx.lineTo(p2.sx, p2.sy);
        ctx.stroke();
      }
    }
  }
}

// ── DRAW MAIN ──
let frameCount = 0;
let fps = 60;
let lastFpsUpdate = 0;

function draw() {
  // Clear
  ctx.fillStyle = '#0a0a0a';
  ctx.fillRect(0, 0, W, H);

  // Draw background stars
  drawStars();

  // Draw main structure
  drawHarmonicStructure();

  // Update info
  const ratioKey = RATIO_KEYS[ratioIndex];
  const ratioData = RATIOS[ratioKey];
  const ratioValue = RATIO_VALUES[ratioIndex];

  const nextIndex = (ratioIndex + 1) % RATIO_KEYS.length;
  const nextKey = RATIO_KEYS[nextIndex];
  document.getElementById('ratio-display').textContent = ratioKey + ' → ' + nextKey;
  document.getElementById('structure-display').textContent = ratioValue > 1.2 ? 'Stable' : 'Forming';
  document.getElementById('energy-display').textContent = ratioValue > 1 ? 'Ascending' : 'Descending';
  document.getElementById('coherence-display').textContent = (92 + 8 * Math.sin(time * 0.01 + ratioValue)).toFixed(0) + '%';

  const scalarText = document.getElementById('scalar-text');
  scalarText.innerHTML = ratioKey + ' — ' + ratioData.desc + ' — <span class="highlight">𝔐</span> = ' + PHI_FULL.substring(0, 14) + '...';
}

// ── ANIMATION LOOP ──
let lastTime = 0;

function animate(timestamp) {
  const delta = timestamp - lastTime;
  lastTime = timestamp;
  
  // Update time
  time += delta * 0.001;

  // Update FPS
  frameCount++;
  if (timestamp - lastFpsUpdate > 1000) {
    fps = Math.round(frameCount * 1000 / (timestamp - lastFpsUpdate));
    document.getElementById('fps-text').textContent = fps;
    frameCount = 0;
    lastFpsUpdate = timestamp;
  }

  // Auto-rotate
  if (autoRotate) {
    rotY += autoRotateSpeed * delta * 0.5;
  }

  // Draw
  draw();

  requestAnimationFrame(animate);
}

// ─── CONTROLS ───
document.getElementById('btn-rotate').addEventListener('click', function() {
  autoRotate = !autoRotate;
  this.classList.toggle('active');
});

document.getElementById('btn-reset').addEventListener('click', function() {
  rotX = 0.3;
  rotY = 0.5;
  ratioIndex = 4;
  document.getElementById('slider-ratio').value = 50;
});

document.getElementById('btn-structure').addEventListener('click', function() {
  showStructure = !showStructure;
  this.classList.toggle('active');
});

document.getElementById('btn-flow').addEventListener('click', function() {
  showFlow = !showFlow;
  this.classList.toggle('active');
});

document.getElementById('slider-ratio').addEventListener('input', function() {
  const val = parseFloat(this.value);
  ratioIndex = Math.floor((val / 100) * (RATIO_KEYS.length - 1));
  ratioIndex = Math.max(0, Math.min(RATIO_KEYS.length - 1, ratioIndex));
});

document.getElementById('view-select').addEventListener('change', function() {
  viewMode = this.value;
});

// ─── KEYBOARD SHORTCUTS ───
document.addEventListener('keydown', (e) => {
  if (e.key === 'r' || e.key === 'R') {
    document.getElementById('btn-rotate').click();
  }
  if (e.key === 's' || e.key === 'S') {
    document.getElementById('btn-structure').click();
  }
  if (e.key === 'f' || e.key === 'F') {
    document.getElementById('btn-flow').click();
  }
  if (e.key === 'ArrowRight') {
    const slider = document.getElementById('slider-ratio');
    slider.value = Math.min(100, parseFloat(slider.value) + 5);
    slider.dispatchEvent(new Event('input'));
  }
  if (e.key === 'ArrowLeft') {
    const slider = document.getElementById('slider-ratio');
    slider.value = Math.max(0, parseFloat(slider.value) - 5);
    slider.dispatchEvent(new Event('input'));
  }
});

// ─── HIDE LOADING ───
setTimeout(() => {
  document.getElementById('loading').classList.add('hidden');
}, 1000);

// ─── START ───
// Set initial active states
document.getElementById('btn-rotate').classList.add('active');
document.getElementById('btn-structure').classList.add('active');
document.getElementById('btn-flow').classList.add('active');

animate(0);
</script>
</body>
</html>
